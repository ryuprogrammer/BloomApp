import Foundation
import SwiftUI

extension Color {
    // メインカラー
    static let main = Color("mainPink")
    static let vipStart = Color("vipTicketStart")
    static let vipEnd =  Color("vipTicketEnd")
    // 料金プランのサイトの背景色
    static let webBackPink = Color("webPink")
}
